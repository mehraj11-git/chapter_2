first_name = "mehraj"
second_name = "khan"
full_name = first_name + "  " + second_name
print(full_name)
print(full_name *4) #multiply

# string use for representing textual data
# string is immutable
# string use to merge to Data 
# string join with string only we cant add number we have to use int method here
print(first_name + str(3)) # no error
print(first_name + 3) # error
# we can do multiply with number but cant add .